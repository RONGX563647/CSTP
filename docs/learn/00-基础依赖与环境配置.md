# 00 基础依赖与环境配置

学习时长: 2-3 天
难度: ⭐⭐☆☆☆
前置知识: Java 基础、Maven 基础、Node.js 基础

---

## 一、项目概述

### 1.1 技术栈总览

**后端技术栈:**

核心框架:
- Java 17 (LTS)
- Spring Boot 3.3.4
- Spring Security (认证授权)
- Spring Data JPA (数据持久化)

通信协议:
- SSE (Server-Sent Events 实时消息)
- WebSocket (实时通信)
- REST API (前后端交互)

数据存储:
- PostgreSQL (生产数据库)
- H2 (开发环境内存数据库)
- Redis (分布式缓存)

消息中间件:
- RabbitMQ (消息队列)

AI集成:
- Spring AI (OpenAI)

其他工具:
- Lombok (代码简化)
- JWT (Token认证 - jjwt 0.12.6)
- Knife4j (API文档)
- Hutool (工具库)
- Aliyun OSS (对象存储)
- Spring Boot DevTools (热部署)

**前端技术栈:**

核心框架:
- Vue 3.5 (Composition API)
- TypeScript 5.8
- Vite 6.2 (构建工具)

UI组件库:
- Element Plus 2.8

状态管理:
- Pinia 3.0

路由管理:
- Vue Router 5.0

HTTP客户端:
- Axios 1.7

测试:
- Playwright (E2E测试)

### 1.2 项目结构

```
LEARN_AISALE/
├── backend/                           # 后端项目
│   ├── src/main/java/com/aisale/backend/
│   │   ├── aspect/                   # AOP切面
│   │   ├── component/                # 组件类
│   │   ├── config/                   # 配置类
│   │   ├── controller/               # REST控制器
│   │   ├── dto/                      # 数据传输对象
│   │   ├── entity/                   # JPA实体类
│   │   ├── exception/                # 异常处理
│   │   │   ├── business/            # 业务异常
│   │   │   └── system/              # 系统异常
│   │   ├── handler/                  # 异常处理器
│   │   ├── interceptor/              # 拦截器
│   │   ├── repository/               # 数据访问层
│   │   ├── security/                 # 安全配置
│   │   ├── service/                  # 业务逻辑层
│   │   └── util/                     # 工具类
│   ├── src/main/resources/
│   │   ├── application.yml           # 主配置文件
│   │   ├── application-dev.yml       # 开发环境配置
│   │   ├── application-prod.yml      # 生产环境配置
│   │   └── logback-spring.xml        # 日志配置
│   ├── tests/                        # API测试脚本
│   └── pom.xml                       # Maven依赖配置
│
├── frontend/                          # 前端项目
│   ├── src/
│   │   ├── api/                      # API接口封装
│   │   ├── components/               # 公共组件
│   │   ├── layouts/                  # 布局组件
│   │   ├── router/                   # 路由配置
│   │   ├── stores/                   # Pinia状态管理
│   │   ├── styles/                   # 样式文件
│   │   ├── utils/                    # 工具函数
│   │   └── views/                    # 页面组件
│   │       ├── user/                # 用户端页面(H5)
│   │       └── admin/               # 管理端页面
│   ├── package.json                  # 前端依赖配置
│   ├── vite.config.ts                # Vite构建配置
│   └── tsconfig.json                 # TypeScript配置
│
└── docs/                              # 项目文档
    ├── learn/                         # 学习文档
    ├── bugfix/                        # Bug修复记录
    └── superpowers/                   # 设计文档
```

---

## 二、后端依赖配置

### 2.1 Maven 核心依赖

文件位置: `backend/pom.xml`

#### 2.1.1 Spring Boot 基础依赖

```xml
<!-- 父POM：统一管理Spring Boot版本 -->
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.4</version>
    <relativePath/>
</parent>

<!-- 核心依赖说明 -->
<properties>
    <java.version>17</java.version>              <!-- 使用Java 17 LTS -->
    <spring-ai.version>1.0.0-M4</spring-ai.version>
    <knife4j.version>4.5.0</knife4j.version>
    <lombok.version>1.18.36</lombok.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
</properties>
```

#### 2.1.2 Web服务依赖

```xml
<!-- Web服务 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<!-- AOP切面支持 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId>
</dependency>

<!-- 参数校验 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

#### 2.1.3 数据持久化依赖

```xml
<!-- JPA数据持久化 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>

<!-- PostgreSQL生产数据库 -->
<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <scope>runtime</scope>
</dependency>

<!-- H2开发环境内存数据库 -->
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
    <scope>runtime</scope>
</dependency>
```

#### 2.1.4 安全与认证依赖

```xml
<!-- Spring Security -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>

<!-- JWT认证 (jjwt 0.12.6) -->
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-api</artifactId>
    <version>0.12.6</version>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-impl</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
<dependency>
    <groupId>io.jsonwebtoken</groupId>
    <artifactId>jjwt-jackson</artifactId>
    <version>0.12.6</version>
    <scope>runtime</scope>
</dependency>
```

#### 2.1.5 缓存依赖

```xml
<!-- Redis分布式缓存 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
```

#### 2.1.6 消息中间件依赖

```xml
<!-- RabbitMQ消息队列 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>
```

#### 2.1.7 AI集成依赖

```xml
<!-- Spring AI - OpenAI -->
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
    <version>${spring-ai.version}</version>
</dependency>
```

#### 2.1.8 工具类依赖

```xml
<!-- Lombok代码简化 -->
<dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <optional>true</optional>
</dependency>

<!-- Hutool工具库 -->
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-all</artifactId>
    <version>5.8.23</version>
</dependency>

<!-- Dotenv环境变量支持 -->
<dependency>
    <groupId>io.github.cdimascio</groupId>
    <artifactId>dotenv-java</artifactId>
    <version>3.0.0</version>
</dependency>
```

#### 2.1.9 对象存储依赖

```xml
<!-- 阿里云OSS SDK -->
<dependency>
    <groupId>com.aliyun.oss</groupId>
    <artifactId>aliyun-sdk-oss</artifactId>
    <version>3.17.4</version>
</dependency>
```

#### 2.1.10 API文档依赖

```xml
<!-- Knife4j OpenAPI文档 -->
<dependency>
    <groupId>com.github.xiaoymin</groupId>
    <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
    <version>${knife4j.version}</version>
</dependency>
```

#### 2.1.11 开发工具依赖

```xml
<!-- DevTools热部署 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
    <scope>runtime</scope>
    <optional>true</optional>
</dependency>
```

#### 2.1.12 测试依赖

```xml
<!-- Spring Boot测试 -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```

### 2.2 Maven 构建配置

```xml
<build>
    <plugins>
        <!-- 编译器插件 -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.13.0</version>
            <configuration>
                <source>17</source>
                <target>17</target>
                <release>17</release>
                <annotationProcessorPaths>
                    <!-- Lombok注解处理器 -->
                    <path>
                        <groupId>org.projectlombok</groupId>
                        <artifactId>lombok</artifactId>
                        <version>${lombok.version}</version>
                    </path>
                </annotationProcessorPaths>
                <fork>true</fork>
                <compilerArgs>
                    <arg>-J--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>
                </compilerArgs>
            </configuration>
        </plugin>

        <!-- Spring Boot Maven插件 -->
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
            <executions>
                <execution>
                    <goals>
                        <goal>repackage</goal>
                    </goals>
                </execution>
            </executions>
            <configuration>
                <excludes>
                    <!-- Lombok不打包到jar -->
                    <exclude>
                        <groupId>org.projectlombok</groupId>
                        <artifactId>lombok</artifactId>
                    </exclude>
                </excludes>
                <jvmArguments>-Dfile.encoding=UTF-8 -Dconsole.encoding=UTF-8</jvmArguments>
            </configuration>
        </plugin>
    </plugins>
</build>
```

---

## 三、前端依赖配置

### 3.1 package.json 依赖

文件位置: `frontend/package.json`

#### 3.1.1 生产依赖

```json
{
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.1",  // Element Plus图标
    "axios": "^1.7.0",                     // HTTP客户端
    "element-plus": "^2.8.0",              // UI组件库
    "pinia": "^3.0.0",                     // 状态管理
    "vue": "^3.5.0",                       // Vue 3框架
    "vue-router": "^5.0.0"                 // 路由管理
  }
}
```

#### 3.1.2 开发依赖

```json
{
  "devDependencies": {
    "@types/node": "^20.0.0",              // Node.js类型定义
    "@vitejs/plugin-vue": "^5.2.0",        // Vite Vue插件
    "typescript": "~5.8.0",                // TypeScript编译器
    "vite": "^6.2.0",                      // 现代构建工具
    "vue-tsc": "^2.2.0"                    // Vue TypeScript检查
  }
}
```

#### 3.1.3 NPM脚本

```json
{
  "scripts": {
    "dev": "vite",                          // 启动开发服务器
    "build": "vue-tsc -b && vite build",    // 类型检查+构建
    "preview": "vite preview",              // 预览生产构建
    "test:e2e": "playwright test",          // 运行E2E测试
    "test:e2e:ui": "playwright test --ui",  // UI模式测试
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  }
}
```

### 3.2 Vite 配置

文件位置: `frontend/vite.config.ts`

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve, dirname } from 'path'
import { fileURLToPath } from 'url'

const __dirname = dirname(fileURLToPath(import.meta.url))

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')  // 路径别名
    }
  },
  server: {
    port: 3000,                        // 开发服务器端口
    proxy: {
      // API代理配置
      '/api': {
        target: 'http://localhost:8090',
        changeOrigin: true
      },
      '/ws': {
        target: 'http://localhost:8090',
        ws: true,
        changeOrigin: true
      },
      '/api/sse': {
        target: 'http://localhost:8090',
        changeOrigin: true,
        headers: {
          Connection: 'keep-alive',
          'Cache-Control': 'no-cache',
          Accept: 'text/event-stream'
        }
      }
    }
  },
  optimizeDeps: {
    include: ['element-plus']
  }
})
```

### 3.3 TypeScript 配置

文件位置: `frontend/tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

---

## 四、后端配置文件

### 4.1 配置文件结构

```
配置文件说明:
  application.yml:               # 主配置文件（公共配置）
  application-dev.yml:           # 开发环境（H2内存数据库）
  application-prod.yml:          # 生产环境（PostgreSQL）
  logback-spring.xml:            # 日志配置
```

### 4.2 application.yml 主配置

文件位置: `backend/src/main/resources/application.yml`

#### 4.2.1 服务器配置

```yaml
server:
  port: 8090                       # 后端服务端口
```

#### 4.2.2 数据库配置

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:aisale;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
```

#### 4.2.3 JPA配置

```yaml
  jpa:
    hibernate:
      ddl-auto: update             # 自动更新表结构
    show-sql: false                # 不打印SQL
    properties:
      hibernate:
        format_sql: true           # 格式化SQL
        dialect: org.hibernate.dialect.H2Dialect
```

#### 4.2.4 AI配置

```yaml
# AI聊天功能
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: ${OPENAI_BASE_URL:}
      chat:
        enabled: false             # 默认禁用AI聊天
```

#### 4.2.5 API文档配置

```yaml
# Knife4j配置
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

knife4j:
  enable: true
  setting:
    language: zh_cn
```

#### 4.2.6 文件上传配置

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 10MB          # 单文件大小限制
      max-request-size: 50MB       # 总请求大小限制
```

### 4.3 application-dev.yml 开发环境

文件位置: `backend/src/main/resources/application-dev.yml`

```yaml
spring:
  # 使用H2内存数据库（开发环境）
  datasource:
    url: jdbc:h2:mem:aisale;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver

  # H2控制台（浏览器访问 http://localhost:8090/h2-console）
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true                 # 打印SQL
    properties:
      hibernate:
        format_sql: true           # 格式化SQL

# 日志配置
logging:
  level:
    root: INFO
    com.aisale: DEBUG              # 项目日志DEBUG级别
    org.hibernate.SQL: DEBUG       # 打印SQL
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # 打印参数
```

### 4.4 application-prod.yml 生产环境

文件位置: `backend/src/main/resources/application-prod.yml`

```yaml
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:aisale}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:                        # HikariCP连接池
      minimum-idle: 5              # 最小空闲连接
      maximum-pool-size: 20        # 最大连接池大小
      idle-timeout: 30000          # 空闲超时（30秒）
      max-lifetime: 1800000        # 连接最大生命周期（30分钟）
      connection-timeout: 30000    # 连接超时（30秒）

  jpa:
    show-sql: false                # 不打印SQL
    hibernate:
      ddl-auto: none               # 生产环境不自动更新

# 日志配置
logging:
  level:
    root: WARN
    com.aisale: INFO
    org.hibernate.SQL: WARN
```

---

## 五、Config配置类详解

### 5.1 Config类总览

```
配置类清单:
  安全配置:
    - SecurityConfig.java:         # Spring Security配置

  通信配置:
    - WebConfig.java:              # Web配置（CORS等）
    - RabbitMQConfig.java:         # RabbitMQ配置

  API文档:
    - Knife4jConfig.java:          # Knife4j文档配置

  AI配置:
    - AiConfig.java:               # AI配置

  缓存配置:
    - RedisConfig.java:            # Redis配置

  文件存储:
    - OssConfig.java:              # 阿里云OSS配置
    - OssProperties.java:          # OSS属性配置
```

### 5.2 SecurityConfig 安全配置

文件位置: `backend/src/main/java/com/aisale/backend/security/SecurityConfig.java`

```java
package com.aisale.backend.security;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;
import lombok.RequiredArgsConstructor;

/**
 * Spring Security配置类
 * 
 * 配置说明:
 * 1. 禁用CSRF（前后端分离架构，使用JWT）
 * 2. 禁用Session（无状态认证）
 * 3. 配置URL访问权限
 * 4. 添加JWT过滤器
 */
@Configuration
@EnableWebSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtAuthenticationFilter;

    /**
     * 配置安全过滤链
     */
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // 禁用CSRF（前后端分离）
            .csrf(csrf -> csrf.disable())

            // 禁用Session（JWT无状态）
            .sessionManagement(session -> 
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))

            // 配置URL访问权限
            .authorizeHttpRequests(auth -> auth
                // 公开接口
                .requestMatchers(
                    "/api/auth/**",
                    "/api/user/products/public/**",
                    "/h2-console/**",
                    "/swagger-ui/**",
                    "/v3/api-docs/**",
                    "/doc.html"
                ).permitAll()

                // Admin接口需要ADMIN角色
                .requestMatchers("/api/admin/**").hasAnyRole("ADMIN", "SUPER_ADMIN")

                // 其他接口需要认证
                .anyRequest().authenticated()
            )

            // 添加JWT过滤器
            .addFilterBefore(
                jwtAuthenticationFilter,
                UsernamePasswordAuthenticationFilter.class
            );

        return http.build();
    }

    /**
     * 密码加密器
     */
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 5.3 WebConfig 跨域配置

文件位置: `backend/src/main/java/com/aisale/backend/config/WebConfig.java`

```java
package com.aisale.backend.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * Web配置
 * 
 * 配置说明:
 * 1. 配置CORS跨域
 * 2. 允许前端开发服务器访问
 */
@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
            .allowedOriginPatterns(
                "http://localhost:*",
                "http://127.0.0.1:*"
            )
            .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
            .allowedHeaders("*")
            .allowCredentials(true)
            .maxAge(3600);
    }
}
```

### 5.4 RedisConfig Redis配置

文件位置: `backend/src/main/java/com/aisale/backend/config/RedisConfig.java`

```java
package com.aisale.backend.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.serializer.GenericJackson2JsonRedisSerializer;
import org.springframework.data.redis.serializer.StringRedisSerializer;

/**
 * Redis配置类
 * 
 * 配置说明:
 * 1. 配置RedisTemplate
 * 2. 使用JSON序列化
 */
@Configuration
public class RedisConfig {

    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory connectionFactory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(connectionFactory);
        
        // 使用String序列化器处理key
        template.setKeySerializer(new StringRedisSerializer());
        // 使用JSON序列化器处理value
        template.setValueSerializer(new GenericJackson2JsonRedisSerializer());
        
        template.afterPropertiesSet();
        return template;
    }
}
```

### 5.5 RabbitMQConfig RabbitMQ配置

文件位置: `backend/src/main/java/com/aisale/backend/config/RabbitMQConfig.java`

```java
package com.aisale.backend.config;

import org.springframework.amqp.core.Queue;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * RabbitMQ配置类
 * 
 * 配置说明:
 * 1. 定义消息队列
 * 2. 配置队列参数
 */
@Configuration
public class RabbitMQConfig {

    /**
     * 聊天消息队列
     */
    @Bean
    public Queue chatMessageQueue() {
        return new Queue("chat.message", true);
    }
}
```

### 5.6 AiConfig AI配置

文件位置: `backend/src/main/java/com/aisale/backend/config/AiConfig.java`

```java
package com.aisale.backend.config;

import org.springframework.ai.chat.client.ChatClient;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * AI配置类
 * 
 * 配置说明:
 * 1. 配置ChatClient Bean
 * 2. 提供AI对话能力
 */
@Configuration
public class AiConfig {

    @Bean
    public ChatClient chatClient(ChatClient.Builder builder) {
        return builder.build();
    }
}
```

### 5.7 Knife4jConfig API文档配置

文件位置: `backend/src/main/java/com/aisale/backend/config/Knife4jConfig.java`

```java
package com.aisale.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * Knife4j配置类
 * 
 * 配置说明:
 * 1. 配置API文档基本信息
 * 2. 配置安全认证方案
 * 
 * 注：主要通过application.yml配置
 */
@Configuration
public class Knife4jConfig {
    // 配置主要通过yaml文件完成
}
```

### 5.8 OssConfig 对象存储配置

文件位置: `backend/src/main/java/com/aisale/backend/config/OssConfig.java`

```java
package com.aisale.backend.config;

import com.aliyun.oss.OSS;
import com.aliyun.oss.OSSClientBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import lombok.RequiredArgsConstructor;

/**
 * 阿里云OSS配置类
 * 
 * 配置说明:
 * 1. 配置OSS客户端
 * 2. 提供文件上传能力
 */
@Configuration
@RequiredArgsConstructor
public class OssConfig {

    private final OssProperties ossProperties;

    @Bean
    public OSS ossClient() {
        return new OSSClientBuilder()
            .build(
                ossProperties.getEndpoint(),
                ossProperties.getAccessKeyId(),
                ossProperties.getAccessKeySecret()
            );
    }
}
```

---

## 六、环境配置对比

### 6.1 环境配置对照表

| 配置项 | dev（开发环境） | prod（生产环境） |
|--------|-----------------|------------------|
| 数据库 | H2内存数据库 | PostgreSQL |
| SQL打印 | 是 | 否 |
| 日志级别 | DEBUG | INFO/WARN |
| H2控制台 | 启用 | 禁用 |
| ddl-auto | update | none |
| 适用场景 | 本地开发调试 | 生产部署 |

### 6.2 如何切换环境

```bash
# 方法1: 命令行参数
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 方法2: 环境变量
export SPRING_PROFILES_ACTIVE=prod
mvn spring-boot:run

# 方法3: application.properties
# 在 src/main/resources/application.properties 中添加：
# spring.profiles.active=dev

# 方法4: JVM参数
java -jar -Dspring.profiles.active=prod backend.jar
```

---

## 七、开发环境搭建

### 7.1 环境要求

**必备环境:**
- JDK: 17+ (推荐17 LTS)
- Maven: 3.8+
- Node.js: 18+ (推荐20 LTS)
- npm: 9+ 

**可选环境:**
- PostgreSQL: 14+ (本地开发)
- Docker: 20+ (容器化部署)
- Redis: 7+ (缓存)
- RabbitMQ: 3.12+ (消息队列)

### 7.2 后端启动

```bash
# 1. 克隆项目
git clone <repository-url>
cd LEARN_AISALE/backend

# 2. 安装依赖
mvn clean install

# 3. 开发环境启动（使用H2内存数据库）
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 4. 访问API文档
# http://localhost:8090/swagger-ui.html
# 或 http://localhost:8090/doc.html

# 5. H2控制台（仅dev环境）
# http://localhost:8090/h2-console
# JDBC URL: jdbc:h2:mem:aisale
# 用户名: sa
# 密码: (空)
```

### 7.3 前端启动

```bash
# 1. 进入前端目录
cd LEARN_AISALE/frontend

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 访问前端页面
# http://localhost:3000

# 5. 生产构建
npm run build

# 6. 预览生产构建
npm run preview

# 7. 运行E2E测试
npm run test:e2e
```

### 7.4 使用Docker启动

```bash
# 1. 启动所有服务
docker-compose up -d

# 2. 查看服务状态
docker-compose ps

# 3. 查看日志
docker-compose logs -f

# 4. 停止服务
docker-compose down
```

### 7.5 测试账号

| 用户名 | 密码 | 角色 | 备注 |
|--------|------|------|------|
| superadmin | 123456 | SUPER_ADMIN | 完整admin权限 |
| admin | 123456 | ADMIN | 普通admin |
| zhangsan | 123456 | USER | 有2个地址，销售商品 |
| lisi | 123456 | USER | 有1个地址，销售商品 |
| wangwu | 123456 | USER | 无地址，仅购买 |

---

## 八、常见问题

### 8.1 依赖问题

**Q: Maven依赖下载慢**

```xml
<!-- 配置国内镜像（~/.m2/settings.xml） -->
<mirrors>
    <mirror>
        <id>aliyun</id>
        <mirrorOf>central</mirrorOf>
        <name>Aliyun Maven</name>
        <url>https://maven.aliyun.com/repository/central</url>
    </mirror>
</mirrors>
```

**Q: npm install失败**

```bash
# 配置国内镜像
npm config set registry https://registry.npmmirror.com

# 清除缓存重试
npm cache clean --force
npm install
```

### 8.2 配置问题

**Q: 如何查看当前生效的配置**

```bash
# 启动后访问
http://localhost:8090/actuator/env

# 或查看启动日志中的Active profiles
```

**Q: 数据库连接失败**

排查步骤:
1. 检查数据库是否启动
2. 检查application.yml中的数据库配置
3. 检查环境变量是否覆盖配置
4. 查看启动日志中的错误信息

### 8.3 端口冲突

```bash
# 查看端口占用
# Windows
netstat -ano | findstr :8090

# 修改端口
# application.yml
server:
  port: 8091
```

### 8.4 AI功能问题

**Q: AI聊天功能如何使用**

1. 在 `.env` 文件中配置OpenAI API Key
2. 设置 `AI_CHAT_ENABLED=true`
3. 重启后端服务

```env
# .env 文件示例
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com
AI_CHAT_ENABLED=true
```

---

## 九、扩展练习

### 练习1：添加自定义配置属性

1. 在application.yml中添加自定义配置
2. 创建@ConfigurationProperties配置类
3. 在Service中注入并使用

### 练习2：配置Redis缓存

1. 安装Redis
2. 配置RedisConfig
3. 使用@Cacheable注解缓存数据

### 练习3：集成RabbitMQ消息队列

1. 安装RabbitMQ
2. 配置RabbitMQConfig
3. 创建消息生产者和消费者

### 练习4：配置阿里云OSS文件上传

1. 注册阿里云账号
2. 创建OSS Bucket
3. 配置OssProperties
4. 实现文件上传功能

---

最后更新: 2026-04-22
文档版本: 1.0
